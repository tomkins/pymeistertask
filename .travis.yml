dist: xenial

language: python

cache: pip

python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"

install:
  - pip install tox-travis

script:
  - tox

stages:
  - lint
  - test

jobs:
  include:
    - stage: lint
      python:
        - 3.7
      script:
        - tox -e lint
